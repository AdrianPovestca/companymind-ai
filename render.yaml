services:
  - type: web
    name: companymind-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT run_dashboard:app
    healthCheckPath: /
    autoDeploy: true
